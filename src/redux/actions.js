import {
  addPoliticians, addPolitician,
  updatePolitician, requestPoliticianDetails
} from './features/politicians/politiciansSlice';

import {
  addZips, addZip, updateZip,
} from './features/zips/zipsSlice';

export {
  addPoliticians, addPolitician,
  updatePolitician, requestPoliticianDetails,
  addZips, addZip, updateZip
}